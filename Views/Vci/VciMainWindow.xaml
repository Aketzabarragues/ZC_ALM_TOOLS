<Window x:Class="ZC_ALM_TOOLS.Views.Vci.VciMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localViews="clr-namespace:ZC_ALM_TOOLS.Views.Vci"
        Title="Suite ZC Tools - Auditoría y Mapeo VCI" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#F4F4F4">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Margin="10">
            <TabItem Header="📦 Configuración y Mapeo VCI">
                <localViews:VciMappingView DataContext="{Binding MappingVM}" />
            </TabItem>
            <TabItem Header="🔍 Auditoría y Dependencias XML">
                <localViews:VciAuditView DataContext="{Binding AuditVM}" />
            </TabItem>
        </TabControl>

        <StatusBar Grid.Row="1" Background="#E9ECEF" BorderBrush="#CED4DA" BorderThickness="0,1,0,0" Padding="5,2">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>

            <StatusBarItem Grid.Column="0" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Margin="5,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#4CAF50"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatusColor}" Value="Red">
                                        <Setter Property="Fill" Value="#F44336"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusColor}" Value="Orange">
                                        <Setter Property="Fill" Value="#FF9800"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding StatusMessage}" 
                               Foreground="{Binding StatusColor}" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>

            <StatusBarItem Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Sincronizando..." 
                               FontSize="11" 
                               Foreground="#666" 
                               Margin="0,0,10,0"
                               VerticalAlignment="Center"
                               Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                    <ProgressBar Width="150" 
                                 Height="14" 
                                 IsIndeterminate="True"
                                 Foreground="#007ACC"
                                 Background="#D0D0D0"
                                 BorderThickness="0"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                        <ProgressBar.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="2"/>
                            </Style>
                        </ProgressBar.Resources>
                    </ProgressBar>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>