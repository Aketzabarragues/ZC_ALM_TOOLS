<UserControl x:Class="ZC_ALM_TOOLS.Views.Vci.VciMappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:ZC_ALM_TOOLS.Core"
             xmlns:viewmodels="clr-namespace:ZC_ALM_TOOLS.ViewModels.Vci" 
             d:DataContext="{d:DesignInstance Type=viewmodels:VciMappingViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
        
        
        <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                CornerRadius="4" 
                BorderThickness="{TemplateBinding BorderThickness}" 
                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005B96"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="🔍 Analizar Workspace Actual" 
                    Command="{Binding AnalyzeProjectCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="240" 
                    Margin="0,0,0,0"
                    Background="#007ACC" 
                    Foreground="White"
                    BorderBrush="#CED4DA"/>

            <Border Grid.Column="1" Background="#E8F4F8" BorderBrush="#B9D1EA" BorderThickness="1" CornerRadius="4" Padding="15,8" Margin="15,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ℹ️ Estado del Análisis: " FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding WorkspaceStatusText}" Foreground="#555" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </StackPanel>
            </Border>
        </Grid>


        <DataGrid Grid.Row="1" ItemsSource="{Binding MappingActions}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  Background="White" 
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      IsEnabled="{Binding IsSelectable}" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Estado" Width="225">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="10,0,5,0">
                                <Ellipse Width="10" Height="10" Fill="{Binding StateColor}" Margin="0,0,8,0" VerticalAlignment="Center"/>

                                <TextBlock Text="{Binding StateText}" FontWeight="SemiBold" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding StateColor}"/>

                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridCell}}" Value="True">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </DataTrigger>

                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Tipo" Binding="{Binding BlockType}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding BlockName}" IsReadOnly="True" Width="250" FontWeight="Bold"/>
                <DataGridTextColumn Header="Ruta (Disco)" Binding="{Binding DiskPath}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="🗑️ Eliminar Mapeos" 
                    Command="{Binding UnmapBlocksCommand}" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="200" Margin="0,0,15,0"/>
            
            <Button Content="💾 Aplicar Mapeos" 
                    Command="{Binding ApplyMappingsCommand}" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="200" Margin="0,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>