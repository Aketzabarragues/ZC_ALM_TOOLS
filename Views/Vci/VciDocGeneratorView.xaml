<UserControl x:Class="ZC_ALM_TOOLS.Views.Vci.VciDocGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:ZC_ALM_TOOLS.Core"
             xmlns:viewmodels="clr-namespace:ZC_ALM_TOOLS.ViewModels.Vci" 
             d:DataContext="{d:DesignInstance Type=viewmodels:VciDocGeneratorViewModel}"
             mc:Ignorable="d"              
             Background="#F0F0F0"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ZC_ALM_TOOLS;component/Resources/GlobalStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Content="Refrescar Elementos" 
                    Command="{Binding LoadItemsCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="190" Margin="0,0,15,0"/>

            <Button Grid.Column="1" 
                    Content="Seleccionar Todo" 
                    Command="{Binding SelectAllCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="160" Margin="0,0,10,0"/>

            <Button Grid.Column="2" 
                    Content="Deseleccionar Todo" 
                    Command="{Binding DeselectAllCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="170" Margin="0,0,0,0"/>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding PlcItems}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Tipo" Binding="{Binding SimpleType}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" IsReadOnly="True" Width="280" FontWeight="Bold"/>
                <DataGridTextColumn Header="Ruta en TIA Portal" Binding="{Binding FolderPath}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Exportar Fuentes y Generar Ayuda" 
                    Command="{Binding GenerateDocCommand}" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="280" Margin="0,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>