<UserControl x:Class="ZC_ALM_TOOLS.Views.Settings.SettingsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             Background="#F0F0F0"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ZC_ALM_TOOLS;component/Resources/GlobalStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="15,10" Background="#F5F6F7" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="💾 Guardar Cambios" 
                            Command="{Binding SaveCommand}" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="200"/>
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="10" Background="White" BorderBrush="#D0D0D0">

            
            <TabItem Header="📂 Rutas Globales">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="25,20">
                        <TextBlock Text="Rutas de Ejecutables y Plantillas" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#003466"/>

                        <TextBlock Text="Ruta Extractor (.exe):" FontWeight="SemiBold" Margin="0,5,0,2"/>
                        <TextBox Text="{Binding GlobalSettings.ExtractorExePath, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                        <TextBlock Text="Plantilla Procesos (SCL):" FontWeight="SemiBold" Margin="0,15,0,2"/>
                        <TextBox Text="{Binding GlobalSettings.ProcessTemplatePath, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                        <TextBlock Text="Ruta DocGenerator (.exe):" FontWeight="SemiBold" Margin="0,15,0,2"/>
                        <TextBox Text="{Binding GlobalSettings.DocGeneratorExePath, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                        <TextBlock Text="Manual Word (Estandar):" FontWeight="SemiBold" Margin="0,15,0,2"/>
                        <TextBox Text="{Binding GlobalSettings.DocWordManualPath, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                        <TextBlock Text="Ruta Fuentes SCL Exportadas:" FontWeight="SemiBold" Margin="0,15,0,2"/>
                        <TextBox Text="{Binding GlobalSettings.DocExportSourcesPath, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                        <TextBlock Text="Ruta Salida Ayuda (HTML):" FontWeight="SemiBold" Margin="0,15,0,2"/>
                        <TextBox Text="{Binding GlobalSettings.DocOutputPath, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="🔧 Categorías de Dispositivos">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Mapeo de dispositivos entre Excel y TIA Portal" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Foreground="#003466"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Devices}" CanUserAddRows="True" CanUserDeleteRows="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="150" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Hoja Excel" Binding="{Binding ExcelSheet}" Width="120"/>
                            <DataGridTextColumn Header="Grupo TIA" Binding="{Binding TiaGroup}" Width="150"/>
                            <DataGridTextColumn Header="Tabla TIA" Binding="{Binding TiaTable}" Width="150"/>
                            <DataGridTextColumn Header="Bloque Datos (DB)" Binding="{Binding TiaDbName}" Width="150"/>
                            <DataGridTextColumn Header="Nombre Array" Binding="{Binding TiaDbArrayName}" Width="120"/>
                            <DataGridTextColumn Header="Clase Modelo" Binding="{Binding ModelClass}" Width="120"/>
                            <DataGridTextColumn Header="Constante Max" Binding="{Binding PlcCountConstant}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="⚙️ Ajustes TIA Portal">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="25,20">
                        <TextBlock Text="Configuración de DBs, Tablas y Sufijos" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#003466"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Tabla Constantes Dispositivos:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBox Text="{Binding DeviceSettings.ConfigTableName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                                <TextBlock Text="Sufijo DB Parámetros Reales:" FontWeight="SemiBold" Margin="0,15,0,2"/>
                                <TextBox Text="{Binding ProcessSettings.SuffixDbReal, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                                <TextBlock Text="Sufijo DB Parámetros Enteros:" FontWeight="SemiBold" Margin="0,15,0,2"/>
                                <TextBox Text="{Binding ProcessSettings.SuffixDbInt, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Nombre Base Procesos XML:" FontWeight="SemiBold" Margin="0,5,0,2"/>
                                <TextBox Text="{Binding ProcessSettings.ProcessName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                                <TextBlock Text="Sufijo Constante Real:" FontWeight="SemiBold" Margin="0,15,0,2"/>
                                <TextBox Text="{Binding ProcessSettings.SuffixConstReal, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>

                                <TextBlock Text="Sufijo Constante Entero:" FontWeight="SemiBold" Margin="0,15,0,2"/>
                                <TextBox Text="{Binding ProcessSettings.SuffixConstInt, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>