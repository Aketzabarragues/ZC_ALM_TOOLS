<UserControl x:Class="ZC_ALM_TOOLS.Views.Generator.GeneratorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:ZC_ALM_TOOLS.Views.Generator"
             mc:Ignorable="d" 
             Background="#F0F0F0"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="SettingsButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Background" Value="#E5E5E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005B96"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="15,10" Background="#F5F6F7" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="📂 Cargar Excel Definición" 
                            Command="{Binding LoadDataCommand}" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="260" Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Archivo seleccionado:" FontSize="10" Foreground="#888"/>
                        <TextBlock Text="{Binding SelectedExcelFile, TargetNullValue='Ningún archivo seleccionado'}" 
                                   Foreground="#555" FontWeight="Medium" FontStyle="Italic"/>
                    </StackPanel>
                </StackPanel>
                
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="10" 
                    IsEnabled="{Binding IsDataLoaded}"
                    Background="White" BorderBrush="#D0D0D0">
            <TabItem Header="🏭 Generador de procesos">
                <views:ProcessGeneratorView DataContext="{Binding ProcessGeneratorVM}"/>
            </TabItem>
            <TabItem Header="🔧 Dispositivos">
                <views:DevicesView DataContext="{Binding DevicesVM}"/>
            </TabItem>
            <TabItem Header="🚨 Parametros y alarmas">
                <views:ParamsAlarmsView DataContext="{Binding ParamsAlarmsVM}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>