<UserControl x:Class="ZC_ALM_TOOLS.Views.Generator.ParamsAlarmsView"             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:ZC_ALM_TOOLS.Core" 
             xmlns:viewmodels="clr-namespace:ZC_ALM_TOOLS.ViewModels.Generator" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ParamsAlarmsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ZC_ALM_TOOLS;component/Resources/GlobalStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>


    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="15">
            <StackPanel>
                <TextBlock Text="FILTROS" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,5"/>
                <TextBlock Text="Proceso de Producción:" Margin="0,0,0,2" Foreground="#555"/>

                <ComboBox ItemsSource="{Binding Processes}"
                          SelectedItem="{Binding SelectedProcess}"
                          Style="{StaticResource ModernComboBoxStyle}"
                          Height="30" Margin="0,0,0,20">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Nombre}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Separator Margin="0,0,0,20" Background="#DDD"/>
                <TextBlock Text="WORKFLOW DE SINCRONIZACIÓN" FontWeight="Bold" Foreground="#005B96" Margin="0,0,0,10"/>
                <TextBlock Text="Paso 1: Comprobaciones" FontWeight="SemiBold" Margin="0,5,0,2" Foreground="#444"/>
                <Button Command="{Binding CompareCommand}"
                        Content="Comparar con PLC"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="40" Margin="0,0,0,10"/>
                
                <TextBlock Text="Paso 2: Actualización" FontWeight="SemiBold" Margin="0,5,0,2" Foreground="#444"/>
                <Button Command="{Binding SyncCommand}" 
                        Content="Sincronizar parametros y alarmas" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="40" Margin="0,0,0,10"/>
                
                <TextBlock Text="Elementos a sincronizar:" FontWeight="SemiBold" Margin="0,5,0,5" Foreground="#555" FontSize="11"/>
                <CheckBox Content="Parámetros Reales" IsChecked="{Binding SelectSyncReales}" Margin="5,0,0,5" Foreground="#333" Cursor="Hand"/>
                <CheckBox Content="Parámetros Enteros" IsChecked="{Binding SelectSyncEnteros}" Margin="5,0,0,5" Foreground="#333" Cursor="Hand"/>
                <CheckBox Content="Alarmas" IsChecked="{Binding SelectSyncAlarmas}" Margin="5,0,0,15" Foreground="#333" Cursor="Hand"/>

                <TextBlock Text="*Sincroniza N_MAX y Comentarios DB" FontSize="10" Foreground="#888" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="{Binding SelectedProcess.Nombre, StringFormat='Detalles del Proceso: {0}'}" 
                           FontSize="18" FontWeight="Light" Foreground="#333" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal">
                    <Border Style="{StaticResource StatusIndicatorStyle}" Tag="{Binding SelectedProcess.StatusPReal}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="N_MAX_P_REAL:"/>
                            <TextBlock Style="{StaticResource StatusTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusIndicatorStyle}" Tag="{Binding SelectedProcess.StatusPInt}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="N_MAX_P_INT:"/>
                            <TextBlock Style="{StaticResource StatusTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusIndicatorStyle}" Tag="{Binding SelectedProcess.StatusAlm}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="N_MAX_ALM:"/>
                            <TextBlock Style="{StaticResource StatusTextStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatusIndicatorStyle}" Tag="{Binding SelectedProcess.StatusAlmHmi}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="N_MAX_ALM_HMI:"/>
                            <TextBlock Style="{StaticResource StatusTextStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>

            <TabControl Grid.Row="1" Background="White" BorderThickness="0,1,0,0" BorderBrush="#DDD">

                <TabItem Header="Parámetros Reales">
                    <DataGrid ItemsSource="{Binding CurrentRealParams}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"  
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#EEE"
                              HeadersVisibility="Column"
                              Background="White">                        

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="DB" Binding="{Binding DbNumber}" Width="80" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Numero" Binding="{Binding Numero}" Width="60" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Descripción" Binding="{Binding ComentarioDB}" Width="Auto" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="Auto" ElementStyle="{StaticResource Params_EstadoCellStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Parámetros Enteros">
                    <DataGrid ItemsSource="{Binding CurrentIntParams}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"  
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#EEE"
                              HeadersVisibility="Column"
                              Background="White">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DDD"/>
                            </Style>

                            <Style TargetType="TextBlock" x:Key="CeldaCentrada">
                                <Setter Property="Padding" Value="5,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="DB" Binding="{Binding DbNumber}" Width="80" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Numero" Binding="{Binding Numero}" Width="60" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Descripción" Binding="{Binding ComentarioDB}" Width="Auto" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="Auto" ElementStyle="{StaticResource Params_EstadoCellStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>


                <TabItem Header="Alarmas">
                    <DataGrid ItemsSource="{Binding CurrentAlarms}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" 
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#EEE"
                              HeadersVisibility="Column"
                              Background="White">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DDD"/>
                            </Style>

                            <Style TargetType="TextBlock" x:Key="CeldaCentrada">
                                <Setter Property="Padding" Value="5,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="DB" Binding="{Binding DbNumber}" Width="80" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Numero" Binding="{Binding Numero}" Width="60" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Descripción" Binding="{Binding ComentarioDB}" Width="Auto" ElementStyle="{StaticResource CeldaCentrada}"/>
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="Auto" ElementStyle="{StaticResource Params_EstadoCellStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

            </TabControl>
        </Grid>
    </Grid>
</UserControl>